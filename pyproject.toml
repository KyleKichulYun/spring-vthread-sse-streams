[tool.ruff]
# 모든 파이썬 파일을 타겟으로 설정
extend-include = ["*.py"]
line-length = 120  # 88자에서 120자로 완화

[tool.ruff.lint]
# E: pycodestyle, F: Pyflakes, I: isort, UP: pyupgrade
select = ["E", "F", "I", "UP"]
ignore = []

[tool.ruff.format]
# Black 스타일 적용
quote-style = "double"
indent-style = "space"
